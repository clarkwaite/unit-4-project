<%= form_with(model: event, local: true) do |form| %>
  <% if event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :venue %>
    <%= form.text_field :venue, id: :event_venue %>
  </div>

  <div class="field">
    <%= form.label :city %>
    <%= form.text_field :city, id: :event_city %>
  </div>

  <div class="field">
    <%= form.label :region %>
    <%= form.text_field :region, id: :event_region %>
  </div>

  <div class="field">
    <%= form.label :country %>
    <%= form.text_field :country, id: :event_country %>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.text_field :date, id: :event_date %>
  </div>

  <div class="field">
    <%= form.label :lineup %>
    <%= form.text_field :lineup, id: :event_lineup %>
  </div>

  <div class="field">
    <%= form.label :tickets_url %>
    <%= form.text_field :tickets_url, id: :event_tickets_url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
